-In the folder with the program should be
 an empty folder "Wallpapers", if not, create
 it. Copy as many picture files as you want
 into this Folder (.bmp, .jpg, .gif), these
 pictures will be shown as Wallpaper.
 It would look best if it already has the
 right resolution.